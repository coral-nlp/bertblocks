name: Docker

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

on:
  push:
    branches: ["main"]
    tags: ["v*.*.*"]
  workflow_dispatch: # Allow manual triggers

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write

    strategy:
      matrix:
        include:
          - image_type: "production"
            dockerfile: "Dockerfile"
            image_suffix: ""
            build_on: "tags" # Only build prod image on tags
          - image_type: "development"
            dockerfile: "Dockerfile.dev"
            image_suffix: "-dev"
            build_on: "always" # Build dev image on every trigger

    steps:
      - name: Check build conditions
        id: should_build
        run: |
          if [[ "${{ matrix.build_on }}" == "tags" && "${{ github.ref_type }}" != "tag" ]]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "Skipping ${{ matrix.image_type }} build (not a tag)"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
            echo "Building ${{ matrix.image_type }} image"
          fi

      # Free up disk, otherwise the docker build process runs out of space
      - name: Free Disk Space
        if: steps.should_build.outputs.skip != 'true'
        uses: jlumbroso/free-disk-space@v1.3.1

      - name: Checkout repository
        if: steps.should_build.outputs.skip != 'true'
        uses: actions/checkout@v4

      # Install the cosign tool except on PR
      # https://github.com/sigstore/cosign-installer
      - name: Install cosign
        if: steps.should_build.outputs.skip != 'true' && github.event_name != 'pull_request'
        uses: sigstore/cosign-installer@59acb6260d9c0ba8f4a2f9d9b48431a222b68e20 #v3.5.0
        with:
          cosign-release: "v2.2.4"

      # Set up BuildKit Docker container builder to be able to build
      # multi-platform images and export cache
      # https://github.com/docker/setup-buildx-action
      - name: Set up Docker Buildx
        if: steps.should_build.outputs.skip != 'true'
        uses: docker/setup-buildx-action@f95db51fddba0c2d1ec667646a06c2ce06100226 # v3.0.0

      # Login against a Docker registry except on PR
      # https://github.com/docker/login-action
      - name: Log into registry ${{ env.REGISTRY }}
        if: steps.should_build.outputs.skip != 'true' && github.event_name != 'pull_request'
        uses: docker/login-action@343f7c4344506bcbf9b4de18042ae17996df046d # v3.0.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Extract metadata (tags, labels) for Docker
      # https://github.com/docker/metadata-action
      - name: Extract Docker metadata
        if: steps.should_build.outputs.skip != 'true'
        id: meta
        uses: docker/metadata-action@96383f45573cb7f253c731d3b3ab81c87ef81934 # v5.0.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}${{ matrix.image_suffix }}
          tags: |
            # For production image (tags only)
            type=ref,event=tag,enable=${{ matrix.image_type == 'production' }}
            type=raw,value=latest,enable=${{ matrix.image_type == 'production' && github.ref_type == 'tag' }}
            # For dev image (always latest on main)
            type=raw,value=latest,enable=${{ matrix.image_type == 'development' && github.ref == 'refs/heads/main' }}
            type=ref,event=branch,enable=${{ matrix.image_type == 'development' }}

      # Build and push Docker image with Buildx (don't push on PR)
      # https://github.com/docker/build-push-action
      - name: Build and push Docker image
        if: steps.should_build.outputs.skip != 'true'
        id: build-and-push
        uses: docker/build-push-action@0565240e2d4ab88bba5387d719585280857ece09 # v5.0.0
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.image_type }}
          cache-to: type=gha,mode=max,scope=${{ matrix.image_type }}

      # Sign the resulting Docker image digest except on PRs.
      # This will only write to the public Rekor transparency log when the Docker
      # repository is public to avoid leaking data.  If you would like to publish
      # transparency data even for private images, pass --force to cosign below.
      # https://github.com/sigstore/cosign
      - name: Sign the published Docker image
        if: steps.should_build.outputs.skip != 'true' && github.event_name != 'pull_request'
        env:
          # https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions#using-an-intermediate-environment-variable
          TAGS: ${{ steps.meta.outputs.tags }}
          DIGEST: ${{ steps.build-and-push.outputs.digest }}
        # This step uses the identity token to provision an ephemeral certificate
        # against the sigstore community Fulcio instance.
        run: echo "${TAGS}" | xargs -I {} cosign sign --yes {}@${DIGEST}
